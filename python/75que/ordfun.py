#ord() function returns the Unicode code point for a given character. 
#It takes a single character as input and returns an integer representing the Unicode code point of that character
a="abc"
for char in a:
    print(char, ord(char))


